【prompt1：实现 MVP 版本】

请根据以下要求，实现一个可用的 AI 舆情分析日报系统，并将项目存储在 Daily AI Insight Engine 文件夹下。

一、基础目标
1. 系统需要能够从新闻/资讯数据源获取 AI 相关信息，形成可用于分析的原始数据。
2. 系统需要提供前端页面，支持数据爬取、日报生成、日报查看等核心操作。
3. 日报生成必须调用 DeepSeek API，不能只做本地规则摘要或静态模板拼接。
4. 项目应是一个可运行的 MVP，优先保证主流程完整、数据可追溯、结果可查看。

二、数据爬取与存储
1. 实现数据爬取功能，数据应保存到数据库中。
2. 每条数据至少包含：标题、正文内容或摘要、来源、发布时间。
3. 数据结构应便于后续清洗、筛选、分析和报告生成。
4. 爬取失败、数据为空、字段缺失等情况需要有基本错误处理，不能导致系统整体崩溃。

三、前端页面
1. 前端需要提供清晰可用的操作入口。
2. 页面至少包含：数据爬取、日报生成、日报查看。
3. 前端不需要复杂视觉效果，但需要能完整跑通业务流程。

四、AI Agent 设计
日报生成需要拆分为多个 Agent，不能将原始数据一次性全部交给模型生成最终报告。

必须包含以下 Agent：
1. 数据清洗 Agent：基于结构化数据模型 Schema，对新闻进行字段抽取、整理和规范化。
2. 今日 AI 领域主要热点 Agent：从结构化数据中识别 Top 3-5 个重要事件。
3. 重要事件深度总结 Agent：对关键事件进行背景说明和影响分析。
4. 趋势判断 Agent：分析技术、应用、政策、资本等方向的趋势。
5. 风险或机会提示 Agent：识别潜在风险、行业机会或投资观察点。
6. 结果校验 Agent：检查各 Agent 输出是否完整、格式是否规范、内容是否与输入数据一致。

五、结构化 Schema 要求
1. 设计一套新闻结构化 Schema，用于描述新闻事件。
2. Schema 字段应体现结构化抽取能力，不能只有 summary。
3. 建议字段包括：标题、来源、发布时间、链接、摘要、主体公司/机构、人物、事件类型、关键词、重要性评分、情绪倾向、可信度、地区、相关领域等。
4. 需要在 README 中说明 Schema 设计思路。

六、日报内容要求
生成的 AI 分析日报至少包括：
1. 信息结构化结果。
2. 今日 AI 领域主要热点 Top 3-5。
3. 重要事件深度总结。
4. 趋势判断。
5. 风险或机会提示。
6. 简单可视化或统计结果。

七、验收标准
1. 能启动后端服务和前端页面。
2. 能完成一次数据爬取并写入数据库。
3. 能调用 DeepSeek API 生成日报。
4. 能在前端查看日报结果。
5. README 中说明数据源、系统设计、AI 使用方式、核心流程和运行方式。


【prompt2：扩展数据源与日期能力】

请对 Daily AI Insight Engine 进行功能拓展，使数据获取和日报生成具备更强的可控性。

一、数据源选择
1. 爬取新闻时，前端需要支持选择不同数据源。
2. 至少实现三种英文来源和三种中文来源。
3. 数据源应优先选择 AI、科技、产业资讯相关站点或聚合源。
4. 不同来源的数据应在数据库中保留 source 字段，便于筛选、追溯和分析。

二、日期选择
1. 爬取新闻时，前端需要支持选择日期或日期范围。
2. 后端应根据用户选择的日期过滤或拉取对应数据。
3. 生成日报时，需要支持选择指定日期生成。
4. 日报与新闻数据需要按 report_date 或 published_at 建立对应关系。

三、数据要求
1. 同一天至少尽量获取 10-20 条 AI 相关新闻。
2. 如果部分来源当天数据不足，应允许通过备用来源补充。
3. 对重复标题、重复链接或高度相似内容进行去重。

四、交互要求
1. 前端显示可选数据源列表。
2. 前端显示日期选择控件。
3. 爬取完成后展示新增新闻数量、过滤数量或失败信息。
4. 生成日报后可以直接查看对应日期的日报。

五、验收标准
1. 能选择中英文数据源进行爬取。
2. 能选择日期进行爬取。
3. 能选择日期生成日报。
4. 数据库中保留来源和日期信息。


【prompt3：补充链接与数据筛选 Agent】

请继续完善 Daily AI Insight Engine，重点解决新闻可追溯性和数据质量问题。

一、新闻链接要求
1. 爬取到的每条新闻必须尽量保留原始链接 url。
2. 数据库中需要保存 url 字段。
3. 前端新闻列表或日报引用中，应提供可点击链接，能够跳转到原始新闻页面。
4. 如果某条数据没有有效链接，需要在存储或展示时做合理降级说明。

二、数据源限制
1. 不要从微信公众平台获取数据。
2. 不使用 wechat2rss、微信公众号镜像源或明显依赖微信内容搬运的来源。
3. 避免获取招聘、广告、课程推广、活动报名、无关科技资讯等非 AI 舆情新闻。

三、数据筛选 Agent
1. 在数据写入数据库前，增加数据筛选 Agent。
2. 数据筛选 Agent 需要判断每条数据是否属于 AI 舆情新闻。
3. 筛选依据至少包括：
   - 是否与人工智能、机器学习、大模型、AI 产品、AI 公司、AI 政策、AI 投融资等主题相关
   - 是否为新闻、事件、评论、产业动态或技术发布
   - 是否排除招聘、广告、课程、纯营销、泛科技无关内容
4. 筛选 Agent 输出应包含：是否保留、判断理由、置信度、事件类型或排除原因。

四、数据处理流程
1. 原始爬取数据先进入候选数据列表。
2. 候选数据经过数据筛选 Agent 验证。
3. 只有通过筛选的数据才写入正式新闻表。
4. 被过滤的数据可记录过滤原因，便于调试和说明。

五、验收标准
1. 前端新闻数据中能看到可点击的原文链接。
2. 系统不会使用微信公众平台作为数据源。
3. 招聘、广告、课程等明显无关内容会被过滤。
4. README 中说明数据筛选 Agent 的作用、判断标准和错误处理方式。


【prompt4：补强今日数据可用性与备用数据源】

请检查 Daily AI Insight Engine 的数据爬取功能是否能够获取指定日期，尤其是当天的 AI 新闻数据，并补强数据源可用性。

一、问题背景
1. 当前系统可能存在指定日期没有抓取到新闻的问题。
2. 部分 RSS 或新闻源更新不稳定，可能导致当天数据不足。
3. 需要提升系统在不同日期、不同来源下的稳定性。

二、功能要求
1. 检查现有所有数据源是否仍然可访问。
2. 对不可用、空数据或经常返回无关内容的数据源进行替换或降级处理。
3. 增加备用数据源，优先选择稳定的 AI/科技新闻来源。
4. 当主数据源数据不足时，自动使用备用来源补充。
5. 爬取结果需要记录来源、发布时间、原文链接、抓取时间和过滤状态。

三、数据质量要求
1. 指定日期应尽量获取 10-20 条 AI 相关新闻。
2. 如果当天新闻不足，需要在接口或前端展示明确提示，而不是静默失败。
3. 过滤掉招聘、广告、课程、活动报名、泛科技无关内容。
4. 对标题重复、链接重复或摘要高度相似的数据进行去重。

四、验收标准
1. 能够选择今天日期并成功爬取 AI 相关新闻。
2. 当某个来源不可用时，系统不会整体失败。
3. 前端能显示爬取数量、过滤数量、失败来源和提示信息。
4. README 中说明数据源补充策略和数据不足时的处理方式。